<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ipca.inventario.cloudmanager">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CloudManager"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".HomeActivity"
            android:exported="false"></activity>

        <activity android:name=".ClientPage"></activity>

        <activity android:name=".ProductPage"></activity>

        <activity android:name=".SalePage"></activity>

        <activity android:name=".AddClient"></activity>

        <activity android:name=".AddProduct"></activity>

        <activity android:name=".AddSale"></activity>
    </application>

</manifest>